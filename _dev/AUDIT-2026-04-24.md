# Full site audit — 2026-04-24

Context: first full-spectrum audit of `bogihorvath.com` since the 2026-04-23 cutover from the React SPA to the Eleventy static site. Scope was "how it can be improved from all aspects" using `C:\claudecode_use\1. HTML Creation\` and `C:\claudecode_use\Bagolykaland.hu NEWSITE 2026\` as reference projects to borrow patterns from. Plan file: `~/.claude/plans/do-a-full-audit-quirky-torvalds.md`.

**Headline finding:** the site was in better shape than a surface pass suggested. `.htaccess` already enforces HSTS + immutable caching + WP legacy 301s + dotfile deny. JSON-LD covered Person + WebSite + WebPage + ProfessionalService + BreadcrumbList + BlogPosting. Eight tagged Playwright specs already ran. Server-side tracking failover at `api/log-event.php` already rate-limited + sanitized. The conversion machinery (sticky mobile CTA, post-download modal, for-recruiters card, 3 inline CV CTAs) was already intact with `data-cta-location` attributes throughout.

So the audit was **not** a rescue. It was closing specific gaps and installing a regression safety-net so future edits do not break what is working.

## Baseline (before this audit)

Lighthouse lab results against `https://bogihorvath.com/`:

| Metric | Desktop | Mobile |
|---|---|---|
| **Performance** | 81 | 48 |
| **Accessibility** | 94 | 94 |
| **Best Practices** | 96 | 96 |
| **SEO** | 100 | 100 |
| LCP | 1.7 s | — |
| CLS | 0.004 | — |
| TBT | 0 ms | — |
| FCP | 1.4 s | — |
| Speed Index | 4.1 s | — |
| Total transfer | 1,369 KiB | — |

Largest drags (from `diagnostics`): **50 KiB unused CSS** (Font Awesome ships full icon catalog; only ~22 icons used), **126 KiB unused JS** (GTM + GA4 + Clarity third-party — not trimmable without removing tracking), and **render-blocking Google Fonts** (mobile specifically). Raw baselines committed at `_dev/lighthouse/lh-home-{desktop,mobile}-baseline.json`.

## Findings by dimension (scores and rationale)

| # | Dimension | Score | Why |
|---|-----------|-------|-----|
| 1 | Conversion / CTAs | 9/10 | Proof-first order intact. 3 inline CV CTAs, sticky mobile, post-download modal, for-recruiters card all present with `data-cta-location`. One minor `og:image:type` MIME bug. |
| 2 | SEO / discoverability | 8/10 | Rich JSON-LD, `llms.txt` + `llms-full.txt`, hreflang en + x-default, RSS alternate. Organization + alumniOf + hasOccupation schema absent. |
| 3 | Performance | 6/10 | No Lighthouse baseline committed. 3 Google Fonts families (Fraunces incl. full SOFT/WONK/opsz axes + Inter Tight 400-800 + JetBrains Mono 500/700). Font Awesome 6.5.1 full CSS loaded for ~22 icons used. |
| 4 | Accessibility | 8/10 | Skip link, ARIA modal, `prefers-reduced-motion` respected. 3 LH issues: testimonial `<h4>` after `<h2>` (heading order skip), logo aria-label mismatched visible text, honeypot inputs missing accessible name. |
| 5 | Mobile UX | 9/10 | Sticky CTA + iPhone 13 webkit Playwright project already configured. |
| 6 | Code quality | 5/10 | **Biome linter explicitly disabled** in `biome.json:27`. No `CLAUDE.md` at project root. 11 pre-existing html-validate errors + 29 stylelint errors sat silently. |
| 7 | Build + CI | 3/10 | **No `.github/workflows/` directory** — tests existed but never ran on push. No Lighthouse regression floor. |
| 8 | Testing | 7/10 | 8 tagged spec files, `_dev/test-fast.js` custom runner, `@axe-core/playwright` installed. Some specs stale from the 2026-04-23 Hormozi restructure (expecting `/Unlock Your Team/` hero text and `.service-card × 9`). |
| 9 | Content / messaging | 8/10 | `recruiterFacts` data driving 10-second-scan card. `llms.txt` well-structured. English only by design. |
| 10 | Security / privacy | 9/10 | `.htaccess` already ships HSTS, X-Frame-Options, X-Content-Type-Options, Permissions-Policy, dotfile deny, `Require all denied` on package/composer/mcp configs, API internal dirs 403'd. One meta-vs-header referrer-policy inconsistency. |

## What shipped — wave by wave

### Wave 1 — bug fixes + baseline hygiene

| Bug | Fix | File |
|---|---|---|
| `og:image:type` says `image/png`, actual OG image is `.webp` | → `image/webp` | `_includes/partials/head.njk:37` |
| Meta referrer-policy (`strict-origin-when-cross-origin`) vs HTTP header (`no-referrer-when-downgrade`) mismatch | Header changed to match meta | `.htaccess:169` |
| Biome linter disabled — no JS lint enforcement | `"enabled": true` + `recommended` rules; fixed `require('os')` → `require('node:os')` | `biome.json:27`, `playwright.config.js:2` |
| No AI-assistant guidance file | Wrote 150-line `CLAUDE.md` covering working-dir invariant, deployment flow, conversion hierarchy, `data-cta-location` invariant, consent/tracking layout, common commands, reference projects | `CLAUDE.md` (new) |
| `/api/log-event.php` alive? | Probed prod: `OPTIONS → 204`, `GET → 405` — confirmed live | — |

**Pre-existing html-validate errors fixed (all surfaced once `npm run check` became a useful gate):**

- Three `<section aria-label="Download Bogi's CV">` landmarks in `inline-cv-cta.njk` were identical → `unique-landmark` error. Now dynamic: `aria-label="Download Bogi's CV — {{ inlineCtaLocation }}"` → `_includes/partials/inline-cv-cta.njk:4`.
- `<div aria-label="AI tools">` in `approach-card__ai-tools` was `aria-label-misuse` on a generic element → `<ul role="group" aria-label="AI tools used">` with `<li>` children → `index.njk` approach-card block.
- `<div class="booking-modal" aria-hidden="true" ...>` wrapping focusable inputs triggered `hidden-focusable`. Root cause: static `aria-hidden="true"` was never toggled by JS. Swapped to `inert` attribute; JS now toggles `inert` on open/close → `_includes/partials/booking-modal.njk:1`, `js/main.src.js:238`.

**Pre-existing stylelint errors resolved:**

- Scope narrowed to `css/**/*.src.css` (minified `style.css` was surfacing spurious `selector-attribute-quotes` errors from the minifier's output).
- `value-keyword-case` rule disabled globally — false positives on legitimate font-family preserved-case names (Fraunces, Inter Tight, Roboto, etc.) and `optimizeLegibility`.
- Source fixes: `font-family: 'Font Awesome 6 Free'` → added `, sans-serif` fallback; `word-break: break-word` → `overflow-wrap: break-word`; 27 auto-fixable issues via `stylelint --fix`.

### Wave 2 — SEO & content polish

- **Person JSON-LD expanded** in `_includes/partials/head.njk`:
  - `worksFor` now an object with S.W.I.F.T. URL + Wikipedia `sameAs`.
  - `alumniOf` array added: NTT Ltd., British Telecom, T-Systems (with URLs + sameAs where applicable).
  - `hasOccupation` → `Occupation` node with `occupationalCategory: "13-1111.00"` (BLS SOC code for Management Analysts), `skills` mirroring `site.knowsAbout`, `experienceRequirements` string.
  - `seeks` → `Demand` node explicitly stating Bogi seeks Senior BA / Transformation Lead / Process Optimization roles (interim, contract, or FT remote).
  - `homeLocation` → Belgium. Keeps existing `nationality: Hungary`.
  - All 4 JSON-LD blocks still parse cleanly.
- **Blog post footer now carries CV conversion.** `pages/blog/post.njk` injects `partials/inline-cv-cta.njk` with `inlineCtaLocation="blog_footer"` before the author footer. The existing "Discuss This Article" button picks up `data-cta-location="blog_footer_contact"`. All 5 blog posts now show CV primary + LinkedIn secondary + contact tertiary in the post footer.
- **Sitemap investigation:** `sitemap.xml` is generated from `sitemap.njk` by Eleventy at build time. Not a duplicate. No change needed.

### Wave 3 — Performance baseline + Web Vitals + a11y

- **Lighthouse baselines committed** at `_dev/lighthouse/lh-home-{desktop,mobile}-baseline.json`. Pattern borrowed from `1. HTML Creation` (dozens of `lh-*.json` artifacts).
- **Font audit:** every `font-weight` value 400-900 is used, can't trim Google Fonts URL without regression. JetBrains Mono used 3× (micro labels). Stay as-is.
- **Lazy-loading audit:** every `<img>` in `pages/`, `_includes/`, and `index.njk` already has a `loading` attribute. No gaps.
- **Web Vitals → dataLayer** added to `js/main.src.js` (`initWebVitals()`). Uses raw `PerformanceObserver` — no `web-vitals` npm dep. Fires `bh_web_vital` dataLayer events at `pagehide` / `visibilitychange hidden` for LCP, CLS (×1000 for int), INP, and one-shot FID. GTM can route these into GA4 as a custom event.
- **Accessibility (Lighthouse):**
  - `heading-order`: testimonial `<h4>` → `<h3>` (section `<h2>` → card `<h3>`) → `index.njk`.
  - `label-content-name-mismatch`: removed `aria-label="{{ site.name }} — home"` from logo — visible text "BogiHorvath" is a sufficient accessible name → `_includes/partials/header.njk:3`.
  - `label`: both honeypot `<input name="botcheck">` inputs now carry `aria-hidden="true" aria-label="Do not fill this field"` → `index.njk` contact form + `_includes/partials/booking-modal.njk`.

### Wave 4 — CI + regression safety-net

- **GitHub Actions** `.github/workflows/ci.yml`:
  - `lint-and-build` job on every PR and push: `npm ci` → `npm run check` → `npm run build` → cache Playwright browsers → `npm run test:smoke`. Uploads report on failure.
  - `full-suite` job, only on push to `main`: adds `test:a11y` + `test:seo` + `test:responsive`.
  - `lighthouse` job on every PR: starts `npx http-server`, uses `treosh/lighthouse-ci-action@v12` against local `/`, `/about/`, `/blog/`.
- **Lighthouse assertions** at `.github/lighthouserc.json` — **regression floors, not aspirations** (set below current baseline so CI doesn't flip red on day 1): Perf ≥ 0.75 warn, Accessibility ≥ 0.9 error, Best Practices ≥ 0.9 error, SEO ≥ 0.95 error, CLS ≤ 0.1 error, TBT ≤ 500 ms warn. Ratchet these up as Wave 5 optimizations land.
- **Visual regression spec** at `_dev/tests/14-visual.spec.js` — mirrors Bagolykaland's `08-visual-regression.spec.js` pattern. Snapshots `/` and `/about/` at 375 px and 1280 px with animations/transitions/scroll-behavior stripped and `document.fonts.ready` awaited. 0.5% diff threshold. New `VISUAL_BREAKPOINTS` helper at `_dev/tests/helpers/pages.js`.
- **New scripts** in `package.json`: `test:visual` (diff against baselines) and `test:visual:update` (regenerate).

### Test specs updated (stale since Hormozi restructure)

- `15-content.spec.js`: hero headline regex `/Unlock Your Team/` → `/EUR 700k\+ saved/`. Services count `9` → `4` (section was compressed). Skill and cert selectors updated from obsolete `.skill-card` / `.cert-category` classes to the current `.skills-strip__row` / `.cert-pill`.
- `13-forms.spec.js`: success-text match `'Message Sent'` → `/Message sent/i` (current copy is lowercase `"Message sent."`).
- `00-smoke.spec.js`: `waitUntil: 'networkidle'` → `'load'`. GTM keeps the network busy enough that `networkidle` never settles on a tracking-heavy site, causing timeouts on slow CI runners.

Result: smoke 32/33 green (one occasional About-page landmark flake — structure verified correct, likely a visibility race under parallel execution; full single-worker + CI run should stabilise it).

## What was deferred (Wave 5 — optional)

These were scoped in the plan but not shipped because each exceeds a "quick win" timebox and carries non-trivial regression risk. Each is documented so a follow-up session can pick one up in isolation.

1. **Font Awesome → self-hosted SVG sprite.** 22 icons actually used (enumerated in the audit). Would recover ~50 KiB unused CSS + a third-party DNS/TLS handshake. Touch every `<i class="fa-*">` site-wide — mechanical but broad. Also kills the CSS `content: '\f058'` pseudo-elements used in `.highlight-pill` which would need inline SVG replacement.
2. **Self-host Fraunces + Inter Tight woff2.** Biggest mobile-perf lever given render-blocking Google Fonts CSS is the main drag pulling mobile Lighthouse to 48. Needs font-file download, subsetting, `@font-face` authoring, removal of both `preconnect`+`preload`+`stylesheet` links from `head.njk:67-78`.
3. **Pre-push hook** via `simple-git-hooks`. Adds a dev-dep and may interfere with existing git flow. CI already covers the same ground.
4. **Initial visual-regression snapshot generation.** First `npm run test:visual` will fail — run `npm run test:visual:update` once locally, commit `_dev/tests/__snapshots__/`, then CI diffs against them.
5. **`/privacy` + `/cookies` pages** and update `tracking.src.js:29` `cookiePolicyUrl: '/#contact'` → `/privacy/`.
6. **`/recruiters/` long-form landing page** that amplifies the current for-recruiters-card into a dedicated URL with JobPosting-ish schema indicating Bogi is *seeking* a role. The `seeks` Demand node added in Wave 2 is a partial step.
7. **Scroll-depth GA4 events** at 25/50/75/100 % to validate the "recruiters only scroll 20 %" assumption with live telemetry. Could ride on the same `initWebVitals()` infrastructure.
8. **Mobile Playwright project + `test:smoke` timeout bumps** — the one About-page flake hints at a visibility race under parallel execution. Worth adding a `test-ios-smoke` project matching the existing webkit-ios config to Playwright's project list for `00-ios-smoke.spec.js` coverage across more pages.

## Non-findings (things that looked wrong but weren't)

- `api/*.php` endpoints on a "static" site — they are live, rate-limited (300 events/min/IP), same-origin-validated, and sanitise their input. Keep them.
- `sitemap.xml` + `sitemap.njk` — not duplicates. `.njk` is the source Eleventy template, `.xml` is the build output.
- `web3formsAccessKey` inlined in rendered HTML — Web3Forms access keys are public by design.
- Consent Mode v2 set to opt-out default (all `granted`) — intentional: B2B recruiter audience, `wait_for_update: 500` ms, `BH_TRACKING_CONFIG.requireConsent = true` flips categories to denied only if the banner is rejected.
- `geo.region: BE` in meta — Bogi lives in Belgium. Correct. `nationality: Hungary` in JSON-LD is consistent.

## Post-deploy verification commands

After `git push origin main` + ~1 min Hostinger auto-pull:

```bash
# og:image:type fix
curl -s https://bogihorvath.com/ | grep 'og:image:type'
# → <meta property="og:image:type" content="image/webp">

# Referrer policy reconciled
curl -sI https://bogihorvath.com/ | grep -i referrer-policy
# → Referrer-Policy: strict-origin-when-cross-origin

# Organization + OccupationalProfile schemas
curl -s https://bogihorvath.com/ | grep -c '"alumniOf"\|"hasOccupation"\|"seeks"'
# → 3

# Blog footer CV CTA
curl -s https://bogihorvath.com/blog/500k-savings/ | grep -c 'data-cta-location="blog_footer'
# → 3 (CV + LinkedIn from inline partial + "Discuss This Article")

# Web Vitals snippet reached production
curl -s https://bogihorvath.com/js/main.js | grep -c 'bh_web_vital'
# → 1
```

Lighthouse re-run after deploy:

```bash
npx lighthouse https://bogihorvath.com/ --preset=desktop \
  --output=json --output-path=_dev/lighthouse/lh-home-desktop-post-audit.json

npx lighthouse https://bogihorvath.com/ --form-factor=mobile \
  --output=json --output-path=_dev/lighthouse/lh-home-mobile-post-audit.json
```

Diff the categories.performance.score between baseline and post-audit. Audit landed no render-path optimisations (those are Wave 5), so expect ≤ 2-point delta in either direction. If any score drops > 5 points, investigate before the next deploy.

## References

- Plan file (incremental during planning): `~/.claude/plans/do-a-full-audit-quirky-torvalds.md`
- Reference — Lighthouse-as-artifact pattern: `C:\claudecode_use\1. HTML Creation\_dev\lh-*.json`
- Reference — visual regression + tiered test scripts: `C:\claudecode_use\Bagolykaland.hu NEWSITE 2026\_dev\tests\08-visual-regression.spec.js` + its `package.json` test scripts
